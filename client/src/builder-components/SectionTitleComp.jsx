import React, { useEffect, useState } from 'react';
import './styles/SectionTitleComp.css';

function SectionTitle({ component }) {
  return (
    <div className='section-title'>
      SectionTitle
    </div>
  )
}

export default SectionTitle;