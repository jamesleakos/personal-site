import styled from 'styled-components';
import '../../styles/constants.css';

export const SpanItemStyled = styled.div`
  display: inline-block;

  span {
    display: inline;
  }

  .actual-span {
    display: inline-block;
    font-size: 1.5rem;
    transition: 0.5s ease;
  }
`;